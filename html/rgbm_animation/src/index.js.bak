import { Normal, compiled_normal, norm2D } from './box-muller';
import ndarray from 'ndarray';
// import { nj as np } from '../numjs'

window.Normal = Normal;
window.ndarray = ndarray;
window.compiled_normal = compiled_normal;
window.norm2D = norm2D;

window.arr = new ndarray(new Float64Array(4), [2, 2]);

var o = ndarray([0, 0, 0], [3]);
norm2D(o, ndarray([1, 2, 3, 4, 5, 6], [3, 2]));
console.log(o.data);

console.log(Normal([10, 10]));
